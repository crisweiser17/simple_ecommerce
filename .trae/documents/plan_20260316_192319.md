I will implement a dedicated product editing page with WYSIWYG editors for descriptions.

### 1. Create New Product Form Template (`templates/admin/product-form.php`)
- Create a new file `templates/admin/product-form.php`.
- Replicate the admin dashboard layout (sidebar, header) to maintain consistency.
- Implement a standard HTML form for product details (Name, SKU, Price, Category, Image, PDF).
- **WYSIWYG Integration**: Add two **Quill** editors for the "Short Description" and "Long Description" fields.
- Add JavaScript to initialize the Quill editors and sync their content to hidden input fields before form submission.

### 2. Register New Route in `index.php`
- Add a new route handler for `/admin/product-form`.
- Implement logic to:
  - Check for an `id` query parameter.
  - If `id` exists, fetch the product data using `getProduct($id)` to populate the form (Edit mode).
  - If no `id`, initialize an empty form (Add mode).
  - Load the `templates/admin/product-form.php` view.

### 3. Update Admin Dashboard (`templates/admin/dashboard.php`)
- Modify the **"Add Product"** button to link to `/admin/product-form` instead of opening a modal.
- Modify the **"Edit"** button in the product list to link to `/admin/product-form?id={id}`.
- Remove the obsolete "Product Modal" HTML and its corresponding Alpine.js logic.
