I will implement the order status control and detailed order management as follows:

### 1. Database Update
- Create a migration script `migrate_orders.php` to add a `tracking_number` column to the `orders` table.

### 2. Backend Logic (`src/orders.php`)
- Update `createOrder` to set the initial status to `ordered`.
- Create `updateOrder($id, $data)` to handle updating customer info, items (JSON), status, and tracking number.
- Ensure `getOrder($id)` retrieves all necessary fields.

### 3. Admin Dashboard (`templates/admin/dashboard.php`)
- Replace the "View Items" dropdown with a "View Order" button that links to a new page: `/admin/order/{id}`.

### 4. New Order Detail Page (`templates/admin/order_detail.php`)
- Create a comprehensive order management interface using Alpine.js.
- **Customer Details**: Editable fields for Name, Email, WhatsApp, Address.
- **Order Items**:
  - List current items with editable Quantity and Price.
  - Allow removing items.
  - Automatically recalculate the Total Amount.
- **Status & Tracking**:
  - Dropdown for status: `ordered`, `paid`, `shipped`, `completed`.
  - Input for `Tracking Number` (conditionally shown or always available).
- **Actions**: "Save Changes" button to update the order.

### 5. Route Handling (`index.php`)
- `GET /admin/order/:id`: Render the new order detail page.
- `POST /admin/order/update`: Process the form submission, re-encode items to JSON, and update the database.

### 6. Customer Account Page (`templates/account.php`)
- Display a read-only list of the user's orders with Status and Tracking Number.
