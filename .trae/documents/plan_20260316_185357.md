I will modify `templates/layout.php` to replace the static navigation menu with a dynamic list of pages fetched from the database.

**Steps:**

1. **Modify** **`templates/layout.php`**:

   * Locate the `<nav>` section (lines 77-84).

   * Keep the "All Products" link but update its class to be conditional based on the current path (`$path === '/'` or `$path === '/home'`).

   * Add a call to `getAllPages()` to retrieve the list of pages from the database.

   * Iterate through the retrieved pages and generate a link for each one (e.g., `<a href="/slug">Title</a>`).

   * Apply the active styling (orange text and bottom border) if the current path matches the page slug, otherwise use the default inactive styling.

   * Remove the hardcoded links ("Peptide Blends", "Popular", etc.).

**Verification:**

* I will verify that the "All Products" link still works and highlights correctly on the homepage.

* I will verify that any pages existing in the database appear in the menu.

* I will verify that clicking a dynamic page link navigates to the correct URL (which is handled by the `default` case in `index.php`).

